由一个小小的图片预加载给自己的jQuery插件编写的启发：
1.首先先分析具体功能，列出可能出现的情况来编写所需的函数及函数的参数，参数可分为默认参数和所需参数，通过调用jQuery自带$.extend({},DEFAULT,Options)来
将函数的参数整合，
2.将构造函数的方法通过prototype添加到原型上供实例使用，
3.最后通过Jquery自带的$.extend或$.fn.extend暴露为插件。
